{% extends 'templates/core.twig' %}

{% block content %}
<div class="page-header header-filter" style="background-image: url('{{ base_url() }}/img/index.png'); background-size: cover; background-position: top center;">
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2 col-xs-10 col-xs-offset-1">
                <div class="card card-signup">
                    <form class="form" method="post" action="{{ path_for('password.change') }}" autocomplete="off">
                        <div class="header header-primary text-center">
                            <h4 class="card-title">Change password</h4>
                        </div>
                        <div class="card-content">
                            <div class="input-group">
									<span class="input-group-addon">
										<i class="material-icons">lock_outline</i>
									</span>
                                <div class="form-group label-floating is-empty {{ errors.current_password ? 'has-error is-focused' : '' }} {{ oldInput.current_password ? 'is-focused' : ''}}">
                                    <label class="control-label">Current password</label>
                                    <input type="password" name="current_password" class="form-control" value="{{ oldInput.current_password }}">
                                    <span class="material-input"></span>
                                    {% if errors.current_password %}
                                        <span class="material-icons form-control-feedback">clear</span>
                                        <span class="help-block">{{ errors.current_password | first }}</span>
                                    {% endif %}
                                </div>
                            </div>
                            <div class="input-group">
									<span class="input-group-addon">
										<i class="material-icons">lock_outline</i>
									</span>
                                <div class="form-group label-floating is-empty {{ errors.new_password ? 'has-error is-focused' : '' }} {{ oldInput.new_password ? 'is-focused' : ''}}">
                                    <label class="control-label">New password</label>
                                    <input type="password" name="new_password" class="form-control" value="{{ oldInput.new_password }}">
                                    <span class="material-input"></span>
                                    {% if errors.new_password %}
                                        <span class="material-icons form-control-feedback">clear</span>
                                        <span class="help-block">{{ errors.new_password | first }}</span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                        <div class="footer text-center">
                            <button type="submit" href="#" class="btn btn-primary btn-simple btn-wd btn-lg">Change password</button>
                        </div>
                        {{ csrf.field | raw }}
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}